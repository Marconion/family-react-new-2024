import { createTheme } from "@mui/material";

export const Theme = createTheme({
  palette: {
    primary: {
      main: "#12372A",
      light: "#FBFADA",
      dark: "#436850",
    },
    secondary: {
      main: "#FBFADA",
    },
  },
});
